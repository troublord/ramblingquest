---
import Layout from "../layouts/BlogPost.astro";
---

<Layout
  title="關於這裡"
  description="工程師毓文的個人空間。技術筆記、碎念、打球觀察，還有一些說不清楚但還是說了的話。"
  pubDate={new Date("2026-06-05")}
  hideNav={true}
>
  <p>一邊漫無目的的晃來晃去，一邊解生命這道宏大的任務</p>

  <hr />

  <div style="display: flex; gap: 2rem; align-items: flex-start;">
    <div style="flex: 1;">
      <h2>我是誰</h2>

      <p>毓文。高雄人，軟體工程師，有膽量瘋狂換工作，沒勇氣主動開話題。</p>

      <p>住在城市的修行家，修身修心修水電(開玩笑的不修水電)</p>

      <p>因為大聲起來像野獸，平常會刻意壓低音量。</p>

      <p>正在練習先做再想，先動再說。</p>

      <p>猴子腦受害者，永遠不去聽專場的甜約翰支持者、Mac Ayres 的頭號聽眾。</p>

      <p>濃縮咖啡機操作手、閱讀清單永遠比人生進度快半拍之男、幽默捕捉者與刪減紀律實踐家。</p>

      <p>常駐愚蠢狀態，正在找法師 Debuff</p>

      <p>硬舉 160 公斤，深蹲 120 公斤，握推<span class="redacted-bar">懂看你餒</span></p>

      <p>喜歡遊戲，喜劇，偶爾看看小說。</p>

      <p>直男，但我覺得自己是變異直男，怎麼說？</p>

      <p>不告訴你</p>
    </div>
    <img
      src="https://i.ibb.co/wNFMBs6k/LINE-ALBUM-2026-01-01-260104-35.jpg"
      alt="毓文"
      style="width: 220px; flex-shrink: 0; border-radius: 4px; object-fit: cover;"
    />
  </div>

  <hr />

  <h2>這裡有什麼</h2>

  <p>這個空間分成四個房間：</p>

  <p><strong>書房</strong> — 閱讀、書摘、長一點的文章。</p>

  <p><strong>吧台</strong> — 碎念、情緒、一時起意的感想。</p>

  <p><strong>工坊</strong> — 技術筆記、工程問題、Side Project 的過程記錄。</p>

  <p><strong>場邊</strong> — 籃球、運動、以及其他跟真實世界比較近的事情。</p>

  <hr />

  <h2>這網站怎麼弄的</h2>

  <p>框架用 Astro，是一個以靜態網站生成（SSG）為主的框架。寫起來很像一般的 HTML 跟元件，但 build 出來是一整組純靜態頁面，沒有後端在跑，載入快，也好維護。</p>

  <p>文章都用 Markdown（.md / .mdx）寫，存檔就是一篇新文章，不用登入任何後台。</p>

  <p>留言區是自己刻的，沒有套現成的留言服務。有新留言時我會收到 Discord 通知，所以不用擔心沒人看到。</p>

  <p>整個網站放在 Netlify 上，部署也在那邊處理。程式碼推到 GitHub 的 master 分支後，Netlify 會自動 build 並上線，不用手動部署。</p>

  <p>設計上想要的是「深夜城市裡散步」的感覺：城市是主視覺，四個房間（書房、吧台、工坊、場邊）是招牌，不是卡片。</p>

  <hr />

  <p>你在這裡不會看到成功學或是任何「讓你進步 5%」的內容。</p>

  <p>會看到我在想什麼，做什麼，讀什麼，可能會看到我搞砸了什麼。</p>

  <p>歡迎進來坐。</p>

  <hr />

  <h2>聯絡我</h2>

  <p>有話想說、發現我哪裡寫錯了，或單純想打個招呼，都可以留言給我。</p>

  <form id="contact-form" class="contact-form" novalidate>
    <p class="contact-honeypot" aria-hidden="true">
      <label for="contact-website">Website</label>
      <input type="text" id="contact-website" name="website" tabindex="-1" autocomplete="off" />
    </p>
    <p>
      <label for="contact-name">名字</label>
      <input type="text" id="contact-name" name="name" maxlength="60" required />
    </p>
    <p>
      <label for="contact-email">Email</label>
      <input type="email" id="contact-email" name="email" maxlength="254" required />
    </p>
    <p>
      <label for="contact-message">訊息</label>
      <textarea id="contact-message" name="message" rows="5" maxlength="2000" required></textarea>
    </p>
    <button type="submit">送出</button>
    <p class="contact-status" id="contact-status" role="status"></p>
  </form>
</Layout>

<script>
  (function () {
    const form = document.getElementById('contact-form') as HTMLFormElement;
    const status = document.getElementById('contact-status') as HTMLElement;
    if (!form || !status) return;
    const submitBtn = form.querySelector('button[type="submit"]') as HTMLButtonElement | null;

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      status.textContent = '';
      status.removeAttribute('data-state');
      if (submitBtn) submitBtn.disabled = true;

      const name = (form.elements.namedItem('name') as HTMLInputElement).value;
      const email = (form.elements.namedItem('email') as HTMLInputElement).value;
      const message = (form.elements.namedItem('message') as HTMLTextAreaElement).value;
      const website = (form.elements.namedItem('website') as HTMLInputElement).value;

      try {
        const res = await fetch('/api/contact', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ name, email, message, website }),
        });
        const data = await res.json();

        if (res.status === 200) {
          form.reset();
          status.textContent = '訊息送出了，謝謝你 ✦';
          status.dataset.state = 'success';
        } else if (res.status === 429) {
          status.textContent = data.error || '送太多次了，請稍後再試';
          status.dataset.state = 'error';
        } else {
          status.textContent = data.error || '送出失敗，請再試一次';
          status.dataset.state = 'error';
        }
      } catch {
        status.textContent = '網路錯誤，請稍後再試';
        status.dataset.state = 'error';
      } finally {
        if (submitBtn) submitBtn.disabled = false;
      }
    });
  })();
</script>

<style>
  .redacted-bar {
    background-color: #1a0f08;
    color: transparent;
    border-radius: 2px;
    padding: 0 0.3em;
  }

  .contact-honeypot {
    position: absolute;
    left: -9999px;
    width: 1px;
    height: 1px;
    overflow: hidden;
  }

  .contact-status {
    margin-top: 0.6rem;
    font-size: 0.9rem;
    min-height: 1.2rem;
  }
  .contact-status[data-state="error"] { color: #b5644f; }
  .contact-status[data-state="success"] { color: #3e6b1f; }

  .contact-form {
    max-width: 480px;
  }

  .contact-form label {
    display: block;
    font-size: 0.9rem;
    color: rgb(var(--gray-dark));
    margin-bottom: 0.3rem;
  }

  .contact-form input,
  .contact-form textarea {
    width: 100%;
    font-family: inherit;
    font-size: 1rem;
    padding: 0.5rem 0.6rem;
    border: 1px solid rgb(var(--gray-light));
    border-radius: 4px;
    box-sizing: border-box;
  }

  .contact-form input:focus,
  .contact-form textarea:focus {
    outline: none;
    border-color: var(--accent);
  }

  .contact-form button {
    background-color: var(--accent);
    color: white;
    border: none;
    border-radius: 4px;
    padding: 0.6rem 1.4rem;
    font-size: 1rem;
    cursor: pointer;
  }

  .contact-form button:hover {
    background-color: var(--accent-dark);
  }
</style>
